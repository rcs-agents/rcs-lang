{
  "name": "rcl-monorepo",
  "version": "0.4.0",
  "type": "module",
  "author": {
    "name": "Saulo Vallory",
    "email": "saulo@tokilabs.io",
    "url": "https://tokilabs.io/"
  },
  "repository": "https://github.com/rcs-agents/rcs-lang",
  "description": "RCL Language Monorepo - ANTLR4 parser and language tools",
  "engines": {
    "bun": ">=1.2.0"
  },
  "packageManager": "bun@1.2.18",
  "workspaces": [
    "apps/*",
    "libs/*",
    "packages/*"
  ],
  "license": "MIT",
  "dependencies": {
    "@antfu/ni": "^25.0.0",
    "@rcs-lang/cli": "workspace:*"
  },
  "private": true,
  "scripts": {
    "setup": "./scripts/setup.sh",
    "build": "moon run :build",
    "dev": "moon run :dev",
    "test": "moon run :test",
    "clean": "moon run :clean",
    "lint": "moon run :lint",
    "format": "moon run :format",
    "typecheck": "moon run :typecheck",
    "changeset": "changeset",
    "changeset:add": "changeset add",
    "changeset:status": "changeset status",
    "check:workspace": "bun scripts/check-workspace-deps.js",
    "check:pre-release": "bun scripts/pre-release-check.js",
    "version": "changeset version",
    "release:prepare": "bun run check:pre-release && bun run check:workspace && bun run build && bun run test && changeset version",
    "release:commit": "git add .changeset packages/*/package.json package.json CHANGELOG.md && git commit -m 'chore: version packages'",
    "release:publish": "bun run check:workspace && changeset publish",
    "release:publish:bun": "bun run check:workspace && ./scripts/publish-with-bun.sh",
    "release:push": "git push --follow-tags",
    "release": "bun run release:prepare && bun run release:commit && bun run release:publish && bun run release:push",
    "release:bun": "bun run release:prepare && bun run release:commit && bun run release:publish:bun && bun run release:push",
    "act": "act",
    "act:ci": "act -W .github/workflows/ci.yml",
    "act:push": "act push",
    "act:pr": "act pull_request",
    "act:list": "act -l",
    "publish:extension": "cd apps/extension && bun run publish:marketplace",
    "publish:extension:package": "cd apps/extension && bun run vsce:package",
    "publish:extension:dry": "cd apps/extension && bun run vsce:publish:dry",
    "docs:build": "moon run docs:build",
    "docs:dev": "moon run docs:dev",
    "docs:preview": "moon run docs:preview"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@playwright/test": "^1.58.0",
    "@types/chai": "^5.2.2",
    "@types/jest-image-snapshot": "^6.4.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^25.0.10",
    "@types/sinon": "^17.0.4",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "@wdio/cli": "^9.17.1",
    "@wdio/local-runner": "^9.17.0",
    "@wdio/mocha-framework": "^9.17.0",
    "@wdio/spec-reporter": "^9.17.0",
    "chai": "^5.2.1",
    "jest-image-snapshot": "^6.5.1",
    "mocha": "^11.7.1",
    "playwright": "^1.58.0",
    "sinon": "^21.0.0",
    "wdio-vscode-service": "^6.1.3",
    "webdriverio": "^9.17.0"
  }
}
