project:
  name: '@rcs-lang/file-system'
  description: 'File system abstraction for RCL'

type: library
language: typescript

dependsOn:
  - '@rcs-lang/core'

tags:
  - typescript
  - test
  - common

tasks:
  build:
    deps:
      - '@rcs-lang/core:build'

  typecheck:
    deps:
      - '@rcs-lang/core:build'

  test:
    deps:
      - build