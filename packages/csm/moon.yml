project:
  name: csm
  description: Conversation State Machine library

type: library
language: typescript

dependsOn:
  - core

tags:
  - tsup
  - test
  - typescript
  - publish
  - common

tasks:
  build:
    deps:
      - core:build
      
  clean:
    command: rm -rf dist coverage
    platform: system