project:
  name: '@rcs-lang/compiler'
  description: 'Compilation pipeline for RCL language'

type: library
language: typescript

dependsOn: []

tags:
  - typescript
  - test
  - common

tasks:
  build:
    deps: []

  typecheck:
    deps: []

  test:
    deps:
      - build
