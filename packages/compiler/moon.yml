project:
  name: '@rcs-lang/compiler'
  description: 'Compilation pipeline for RCL language'

type: library
language: typescript

dependsOn:
  - '@rcs-lang/core'
  - '@rcs-lang/csm'
  - '@rcs-lang/file-system'
  - '@rcs-lang/validation'

tags:
  - typescript
  - test
  - common

tasks:
  build:
    deps: []

  typecheck:
    deps: []

  test:
    deps:
      - build
