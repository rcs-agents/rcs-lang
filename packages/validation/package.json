{
  "name": "@rcs-lang/validation",
  "version": "0.3.3",
  "description": "Validation pipeline for RCL language",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "moon run validation:build",
    "dev": "moon run validation:dev",
    "test": "moon run validation:test",
    "test:watch": "moon run validation:test-watch",
    "test:coverage": "moon run validation:test-coverage",
    "typecheck": "moon run validation:typecheck",
    "clean": "moon run validation:clean",
    "lint": "moon run validation:lint",
    "format": "moon run validation:format"
  },
  "dependencies": {
    "@rcs-lang/core": "workspace:*",
    "@rcs-lang/parser": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  },
  "files": [
    "dist/**/*"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  }
}
